import React from 'react'
import { Box } from '../../components/index';

const Slide = ({ mainPhoto, miniPhotos, title, description, index }) => {
    return (
        <Box>

        </Box>
    )
}

export default Slide
